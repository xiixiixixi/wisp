# Wisp 中英文覆盖检查

检查日期：2026-09-08。范围包括桌面主应用、设置、原生菜单代码，以及仓库内扩展源码；不包含市场网站或文档站。

## 结论

语言设置只保留 **中文 / English**。主应用已修复本轮发现的大量缺失词条和硬编码界面文案，但**不能认定整个 Wisp 已达到 100% 双语覆盖**：独立扩展仍存在明确遗漏，动态生成的文案和第三方组件也不能仅凭静态扫描保证完整。

## 本轮完成

- 统一语言选项、资源注册和保存值；`zh-CN`、`en_US` 等值会规范化，旧的不支持语言回退到中文。不再注册日语或印尼语资源。
- 补齐设置、文件浏览、搜索、预览、比较、重命名、冲突处理、聊天、Agent、权限说明等主应用界面的中英文词条。
- 修正计数句子和英文单复数；日期、数字、持续时间跟随应用语言，而不是仅跟随操作系统。
- 已打开的错误界面、权限标签及无障碍标签随语言更新；文件名、路径、用户文档、模型输出保持原文。
- 文件类型和内置快捷键说明使用翻译，不再直接显示后端英文标识。
- macOS 应用菜单增加语言同步，保持现有快捷键处理。已通过原生编译检查，**尚未重新打包安装或验证原生菜单的运行时切换**。

本轮采用 Impeccable 的国际化健壮性检查，重点处理完整句子、动态切换和无障碍文本，没有扩展到视觉重设计。其设计检测器报告的已有视觉建议未纳入本轮修改。

## 验证结果

- 核心扫描：450 个 TS/TSX 文件，中英各 3639 个词条（已扣除教程、退役设置及版本快照等源码与文案）；词条集合差异、插值参数差异、可静态解析的缺失引用均为 0。
- 仍列出 107 个核心 JSX 字面量候选，包含产品名、代码、格式示例等，不能直接视为 107 个翻译缺陷。扫描不覆盖所有动态键、运行时字符串或第三方内部界面。
- 浏览器实测：语言下拉只有两个选项，原 12 个设置分类完成双语切换检查；后续按用户要求依次收敛为 9、6、5 个分类。当前中英文导航只保留通用、文件浏览器、索引、快捷键、关于，已移除版本管理、扩展市场、备份与恢复、审计日志、无障碍。此前已验证文件类型、快捷键、预览及英文索引时间单位，检查后恢复中文设置。
- 专项回归：70/70 通过，涵盖词库、语言设置、动态切换、错误提示、冲突与比较、原生菜单桥接及文本编辑快捷键。
- 较广回归（组件目录及语言设置）：1480 项，1375 通过、105 失败。相同范围的 HEAD 对照为 1457 项、1343 通过、114 失败；本轮失败名称均已存在于 HEAD 对照中。**这不是全部测试通过**，也不是修改前脏工作区的精确快照。
- TypeScript、生产前端构建、Rust `cargo check --lib --bin wisp` 通过；修改涉及的前端文件 ESLint 无错误，仍有既有警告。构建保留已有包体积等警告。

Demo 无后端的文件版本等请求可能仍显示原始错误。已知、由应用产生的预览错误会翻译；未知系统、后端、解码器诊断保留原始信息，避免错误改写。

## 尚未覆盖：独立扩展

`packages/extensions/*/src` 扫描了 25 个 TS/TSX 文件，其中 15 个界面文件有 218 个英文 JSX 文案候选。它们是仓库内扩展，**并非全部属于第三方**，不能用主应用词库检查通过来代表其已翻译。

| 扩展目录 | 字面量候选 |
| --- | ---: |
| 3d-viewer | 5 |
| architect | 14 |
| backup | 17 |
| batch-image | 23 |
| claude-code | 7 |
| code-editor-extension | 9 |
| collaboration | 33 |
| docker | 15 |
| folder-stats | 8 |
| git-extension | 21 |
| image-editor | 11 |
| problems-panel | 7 |
| software-finder | 1 |
| sqlite-browser | 25 |
| ssh | 22 |

例如 SSH 的密码提示、Docker 的不可用提示、Git 的空状态和图片编辑操作仍是英文。计数仅针对 JSX 文本；还需检查属性、运行时拼接、第三方编辑器菜单及扩展元数据。

后续闭环应让扩展 SDK 提供宿主语言读取和变更订阅，再逐个接入独立词库、重建扩展并在真实宿主验证。当前未修改 SDK 契约，也未声称这些扩展已经修复。

## 可重复检查

```sh
pnpm i18n:check
node scripts/audit-i18n.mjs --json
node scripts/audit-i18n.mjs --extensions
```

主检查会因词条差异、插值不一致或可解析的缺失引用而失败；扩展命令提供覆盖报告，不代表已通过完整翻译验收。
