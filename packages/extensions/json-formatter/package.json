{
  "name": "wisp-json-formatter",
  "version": "1.0.0",
  "description": "Format, minify, and validate JSON files from context menu",
  "main": "dist/index.js",
  "type": "module",
  "scripts": {
    "build": "esbuild src/index.tsx --bundle --format=iife --outfile=dist/index.js --external:react --external:react-dom --external:@wisp/extension-sdk --target=es2020 --jsx=transform",
    "dev": "touch .hotreload && esbuild src/index.tsx --bundle --outfile=dist/index.js --watch --format=iife --external:react --external:react-dom --external:@wisp/extension-sdk --define:process.env.NODE_ENV=\\\"development\\\"; rm -f .hotreload"
  },
  "devDependencies": {
    "esbuild": "^0.20.0",
    "typescript": "^5.2.0"
  },
  "wisp": {
    "id": "wisp-json-formatter",
    "name": "JSON Formatter",
    "displayName": "JSON Formatter",
    "description": "Format (pretty-print), minify, and validate JSON files. Right-click any .json file to access these tools.",
    "version": "1.0.0",
    "author": "Wisp Team",
    "category": "action",
    "icon": "braces",
    "keywords": ["action", "json", "format", "minify", "validate", "pretty-print"],
    "permissions": ["file:read", "file:write", "ui:notifications"],
    "activationEvents": ["onStartup"],
    "contributes": {
      "commands": [
        { "command": "format", "title": "Format JSON", "category": "JSON Formatter" },
        { "command": "minify", "title": "Minify JSON", "category": "JSON Formatter" },
        { "command": "validate", "title": "Validate JSON", "category": "JSON Formatter" }
      ],
      "context_menus": [
        { "command": "wisp-json-formatter.format", "when": "fileExtension:.json", "group": "tools" },
        { "command": "wisp-json-formatter.minify", "when": "fileExtension:.json", "group": "tools" }
      ]
    }
  }
}
